# Skiloom 0.1.x 开发交接

日期：2026-09-16

## 下一阶段焦点

继续 `0.1.0 — Domain Foundation` 实施，不重新讨论已经关闭的 v0 架构。当前第一优先是完成 GitHub Issue #38 `SKILL.md` Package admission 的提交与 CI 收尾，然后按 blocker 推进 #39；#40 已经独立解锁，可以并行实施。

## 权威入口

不要在本交接文档复制产品规则；实现时直接读取以下权威材料：

- Canonical Spec：GitHub #31 `Spec: Skiloom v0 implementation and 0.x release plan`
- 产品规范：`docs/design/skiloom-v0-product-contract.md`
- 实施计划：`docs/planning/skiloom-v0-implementation-plan.md`
- 版本路线：`docs/planning/skiloom-v0-version-roadmap.md`
- 当前里程碑：`0.1.0 — Domain Foundation`
- 当前票：GitHub #38 `0.1.0: implement SKILL.md package admission`
- 已并行解锁：GitHub #40 `0.1.0: implement Release Version Requirement grammar and matching`

## 已完成

- #36 Node/TypeScript repository foundation 已完成并关闭；提交 `d2e8591`。GitHub Actions Node 22/24 均通过。
- #37 structured errors + canonical GitHub coordinates 已完成并关闭；提交 `09d1186`。GitHub Actions Node 22/24 均通过。
- 仓库已经从设计期切换到 0.1.x 实施期；入口文档调整提交 `9ab8491`。
- #36–#42 已作为 #31 的 sub-issues 创建，并建立真实 dependency/blocker 关系。

## 当前工作树：#38 未提交现场

当前 `main` 与 `origin/main` 在最后一次同步时一致，但工作树存在 #38 的未提交修改：

- `package.json` / `package-lock.json`：新增 YAML parser runtime dependency；当前安装版本 `yaml@2.9.1`。
- `src/domain/package/`：`SKILL.md` Package admission 实现。
- `behavior-fixtures/package-admission/`：离线行为 fixture。
- `test/behavior/domain/package-admission.test.ts`
- `test/unit/domain/package.test.ts`

最近一次 `npm run check --silent`：12 tests / 12 pass / 0 fail。

不要丢弃或重写这些未提交改动；先用 `git status` / `git diff` 审阅当前实现，然后完成 #38 验收。

## #38 下一步

1. 对照 GitHub #38 acceptance criteria 审阅当前 diff，尤其保持票边界：这里只做 Package admission，不做 repository discovery、dependency resolution 或 Skiloom manifest parsing。
2. 重跑 `npm run check`，并验证 Node 22 与 Node 24；当前代码只完成了本机完整 check，#38 的最低 Node 双线复测尚未形成提交后的 CI 证据。
3. 使用项目要求的 guarded commit：
   `uv run ~/.agents/skills/akira-guard/scripts/guard.py commit -m 'FEAT: (domain) ...'`
4. 推送并等待 GitHub Actions Node 22/24 成功后关闭 #38。
5. #38 关闭后，把 #39 `parse package and repository metadata with strict schemas` 解锁并加 `ready-for-agent`。
6. #40 已经是 `ready-for-agent`，不依赖 #38，可以另一路并行开发。

## 后续 0.1.0 队列

当前依赖主链：

`#38 -> #39 -> #41 -> #42`

独立分支：

`#37 -> #40`（#37 已关闭，因此 #40 当前可执行）

不要机械扩大范围到 0.2.x Resolver；0.1.0 先完成 Package admission、metadata、discovery、Snapshot/Digest、Release Requirement 的离线领域基础。

## GitHub / 网络注意事项

本次会话 GitHub HTTPS/GnuTLS 多次瞬时断开；GitHub SSH 443 可用。若普通 `git push` 再次出现 `gnutls_handshake()`，可临时使用：

`git push ssh://git@ssh.github.com:443/Akira-TL/skiloom.git main`

不要因此修改永久 remote。GitHub API/GraphQL 也偶有 EOF，重试即可；不要把网络失败误判成代码或 CI 失败。

## 工程纪律

- 每张 executable ticket 一个独立、可验证、可回滚目的；完成后先 commit/CI/close，再进入下一个目的。
- 正式提交必须经过 `akira-guard`，不要直接 `git commit`。
- 产品错误继续使用 structured value；测试比较 code + required facts，不锁死自然语言 message。
- 0.1.x 核心 behavior fixtures 必须离线，不依赖 live GitHub。
- 不要重新引入 Project Lock、frozen、AKM alias、通用 Provider/Plugin/DI 框架或 npm semver authority。
- 当前架构 Gate 已关闭；发现真正规范矛盾时才回到设计，不因实现偏好重新开架构讨论。

## Suggested skills

- `engineering/implement`：继续执行 #38 收尾，并按 blocker 实施 #39/#40 等 executable tickets。
- `engineering/tdd`：在补充 Package admission、metadata、discovery、requirement fixture 时坚持先把可观察行为固定下来。
- `engineering/code-review`：每张 ticket 关闭前分别检查 Spec fulfillment 与项目工程标准，避免一个轴通过掩盖另一个轴失败。

## 接手成功标志

下一位 Agent 读取本文件后，第一件事应看到 #38 的未提交工作树，并从“审阅现有 diff + 双 Node 验证”继续，而不是重新创建工程骨架、重新拆票或重新规划 v0。
